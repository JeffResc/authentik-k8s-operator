---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: authentiksamlapplications.goauthentik.io
spec:
  group: goauthentik.io
  names:
    kind: AuthentikSAMLApplication
    listKind: AuthentikSAMLApplicationList
    plural: authentiksamlapplications
    singular: authentiksamlapplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Status
      type: string
    - jsonPath: .status.applicationUid
      name: Application UID
      type: string
    - jsonPath: .status.providerId
      name: Provider ID
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AuthentikSAMLApplication is the Schema for the authentiksamlapplications
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AuthentikSAMLApplicationSpec defines the desired state of
              AuthentikSAMLApplication
            properties:
              group:
                description: Group is the application group in Authentik
                type: string
              metaDescription:
                description: MetaDescription is the application description
                maxLength: 1024
                type: string
              metaLaunchUrl:
                description: MetaLaunchURL is the URL to launch the application
                maxLength: 2048
                type: string
              metaPublisher:
                description: MetaPublisher is the application publisher
                maxLength: 256
                type: string
              name:
                description: Name is the display name of the application in Authentik
                maxLength: 128
                minLength: 1
                type: string
              openInNewTab:
                default: true
                description: OpenInNewTab opens the application in a new browser tab
                type: boolean
              policyEngineMode:
                default: any
                description: PolicyEngineMode determines how policies are evaluated
                enum:
                - all
                - any
                type: string
              provider:
                description: Provider configures the SAML provider settings
                properties:
                  acsUrl:
                    description: ACSUrl is the Assertion Consumer Service URL where
                      Authentik sends SAML responses
                    minLength: 1
                    type: string
                  audience:
                    description: |-
                      Audience is the value of the audience restriction field.
                      When left empty, no audience restriction will be added.
                    type: string
                  authorizationFlow:
                    description: AuthorizationFlow is the flow used for authorization
                    type: string
                  digestAlgorithm:
                    default: http://www.w3.org/2001/04/xmlenc#sha256
                    description: DigestAlgorithm is the algorithm used to compute
                      the digest
                    enum:
                    - http://www.w3.org/2001/04/xmlenc#sha256
                    - http://www.w3.org/2000/09/xmldsig#sha1
                    type: string
                  invalidationFlow:
                    description: InvalidationFlow is the flow used for session invalidation/logout
                    type: string
                  issuer:
                    description: Issuer is the EntityID of the IdP. When left empty,
                      defaults to the Authentik base URL.
                    type: string
                  propertyMappings:
                    description: PropertyMappings is a list of property mapping UUIDs
                      to apply
                    items:
                      type: string
                    type: array
                  signatureAlgorithm:
                    default: http://www.w3.org/2001/04/xmldsig-more#rsa-sha256
                    description: SignatureAlgorithm is the algorithm used to sign
                      SAML responses
                    enum:
                    - http://www.w3.org/2001/04/xmldsig-more#rsa-sha256
                    - http://www.w3.org/2000/09/xmldsig#rsa-sha1
                    - http://www.w3.org/2000/09/xmldsig#dsa-sha1
                    type: string
                  signingKeypair:
                    description: SigningKeypair is the name of the certificate keypair
                      used to sign responses
                    type: string
                  spBinding:
                    default: redirect
                    description: SPBinding configures how the SAML response is delivered
                      to the SP
                    enum:
                    - redirect
                    - post
                    type: string
                required:
                - acsUrl
                - authorizationFlow
                - invalidationFlow
                type: object
              secret:
                description: Secret configures the output Kubernetes secret
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the secret
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to add to the secret
                    type: object
                  name:
                    description: |-
                      Name is the name of the secret to create
                      Defaults to {CR name}-oauth or {CR name}-saml depending on type
                    type: string
                  template:
                    description: |-
                      Template is a Go template for custom secret data
                      If not specified, a default template is used
                    type: string
                type: object
              slug:
                description: |-
                  Slug is the URL-friendly identifier for the application
                  Defaults to metadata.name if not specified
                maxLength: 128
                pattern: ^[a-z0-9-]+$
                type: string
            required:
            - name
            - provider
            type: object
          status:
            description: AuthentikSAMLApplicationStatus defines the observed state
              of AuthentikSAMLApplication
            properties:
              applicationUid:
                description: ApplicationUID is the Authentik internal UID for the
                  application
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the last observed generation of
                  the resource
                format: int64
                type: integer
              providerId:
                description: ProviderID is the Authentik internal ID for the SAML
                  provider
                format: int32
                type: integer
              secretName:
                description: SecretName is the name of the generated Kubernetes secret
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
